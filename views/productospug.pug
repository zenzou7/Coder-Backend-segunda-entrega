html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title= "Document"
    body 
        h1="listado de productos desde pug"
        a(href="/pug/form")="formulario desde pug" 
        div 
            for item in products
                div 
                    p= item.id 
                    p= item.title 
                    p= item.price
                    img(src=item.thumbnail)
